# 要件定義・設計

大学レポートの「作成 → 保管 → 再利用」を一本の流れにし、卒業論文まで使い続けるためのシステム。

## 1. 目的とスコープ

課題情報の管理、Claude による本文生成、文献の蓄積、確定版の Notion 保管を一体化する。

**スコープ外**（現時点）: 成績・フィードバック管理、LMS への提出操作、共同編集。

## 2. 利用者と規模

| 時期 | 対象 | 想定本数 |
| --- | --- | --- |
| 3年秋（2026-秋）〜3年冬 | 履修科目のレポート | 数本〜十数本 |
| 4年（2027年度） | ゼミのみ、履修科目ゼロ | ごく少数 |
| 4年通期 | 卒業論文 | 1本（章立て・長期・文献多数） |

レポートの処理量は小さい。設計の重心は大量さばきではなく、**卒論に向けた文献資産の蓄積品質**に置く。
すなわち課題DBは簡素に保ち、文献DBに設計コストを集中させる。

## 3. 実行方式

- **Claude Code セッションがオペレーター**。指示を受け、判断し、文章を書く
- **決定的な処理はスクリプトに切り出す**。Notion 書き込み、DOI 照会、引用整形、字数計算、PDF 抽出
- Claude はスクリプトを呼ぶ

プロンプトのブレによる書式の揺れを排しつつ、完全自動パイプライン（ポーリング・エラー処理・再実行）の
実装コストを回避する折衷。

## 4. 版管理

- 草稿は Git。`reports/<年度学期>/<科目>_<課題名>/draft.md` をコミット単位で管理
- Notion には確定版のみ。課題ページの本文に `final.md` を流し込む
- 課題DBの「確定コミット」に対応コミットハッシュを持たせ、Notion から Git 履歴を辿れるようにする

## 5. 引用形式

第一実装は **APA（著者-年式）＋和文文献対応**。この分野の主要誌（Journal of Finance、
Journal of Accounting and Economics、The Accounting Review、『會計』『会計プログレス』等）が
著者-年式であるため。課題側で形式指定がない場合のデフォルトも APA。

引用形式は課題ごとに変わるため、文献DBには**形式に依存しない生データのみ**を保存し、
引用文字列は `lib/citation.py` が都度整形する。形式ごとの文字列プロパティは持たない。

## 6. 出典検証（最重要の安全装置）

生成を自動寄りで回す構成では、**存在しない文献が引用に混ざること**が最大の実務リスクである。
提出後に発覚すると取り返しがつかない。そこで:

- 文献DBの「出典検証済」は、DOI 照会が成功したか PDF 実物を確認したものにのみ立てる
- `push_final.py` は最終稿の引用を走査し、未検証の文献が含まれる場合は Notion への書き込みを中止する
- ダッシュボードに「⚠️ 未検証の文献」ビューを常設する

このルールが機械的に効いている限り、この危険はほぼ消える。

## 7. AI 利用の記録

生成のたびに **使用モデル・生成日時・指示概要** を `meta.json` と課題DBに記録する。
多くの大学が卒論で AI 利用の申告を求めるため、後から遡って作れない項目を最初から残しておく。

## 8. 卒業論文

Notion ページツリーで管理する（DB ではない）。文献DBのみ共有し、「使用先章」プロパティで章と文献を紐付ける。
実証研究を想定し、骨組みに「10 データ」「11 分析ノート」を含める。章立て（序論・先行研究・方法・
分析・考察・結論）はテーマ確定後に追加する。

章の作業段階は 構想 → 執筆 → 指導反映待ち → 固め。

## 9. ディレクトリ構成

```
lib/
  config.py       Notion ID・環境変数
  notion.py       Notion API クライアント
  citation.py     引用整形（APA / SIST02）
  refs.py         DOI・ISBN からの書誌取得
  pdf_ingest.py   PDF からのテキスト抽出
scripts/
  new_assignment.py  課題を Notion と Git の両方に作る
  add_reference.py   DOI/ISBN/手動で文献DBに登録
  push_final.py      確定版を Notion に書き込む（出典検証を含む）
prompts/
  report_generate.md  レポート生成の定型プロンプト
  report_review.md    推敲・検証の定型プロンプト
reports/<年度学期>/<科目>_<課題名>/
  assignment.md  課題要件
  draft.md       草稿
  final.md       確定版
  meta.json      使用モデル・生成日時・指示概要・Notion page id
thesis/chNN.md
analysis/
```

## 10. 未決事項

- 科目セレクトの選択肢は今学期の実際の履修科目に合わせて調整する
- SIST02 の番号引用は APA 実装後に追加する
- 卒論の章立てはテーマ確定後に決める
